{
  "version": 2,
  "builds": [
    { "src": "src/*", "use": "@vercel/static-build" },  
    { "src": "api/*.ts", "use": "@vercel/node" }        
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/$1" },          
    { "src": "/(.*)", "dest": "src/$1", "continue": true } 
  ]
}